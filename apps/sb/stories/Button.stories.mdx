import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button } from "@hyezo/ui";

export const Template = args => (
  <div className="grid place-items-center">
    <Button {...args}>Button</Button>
  </div>
);

<Meta
  title="UI/Button"
  component="Button"
  argTypes={{
    color: {
      control: "radio",
      options: ["twitter", "orange", "red", "blue", "black"],
      description: "버튼의 색상을 정합니다",
      table: {
        type: { summary: "string" },
        defaultValue: { summary: "twitter" },
      },
    },
    size: {
      control: "radio",
      options: ["xs", "sm", "md", "lg"],
      description: "버튼의 크기를 정합니다",
      table: {
        type: { summary: "string" },
        defaultValue: { summary: "xs" },
      },
    },
    fullWidth: {
      control: "boolean",
      description: "`true`일 경우 부모 요소의 전체 넓이를 차지합니다",
      table: {
        type: { summary: "boolean" },
        defaultValue: { summary: false },
      },
    },
    outline: {
      control: "boolean",
      description: "`true`일 경우 배경색이 투명해지고 테투리만 표시됩니다",
      table: {
        type: { summary: "boolean" },
        defaultValue: { summary: false },
      },
    },
  }}
/>

# Button

---

버튼 컴퍼넌트입니다.  
props로 `href`를 받을 경우, next.js의 `<Link>` 컴퍼넌트로 랩핑됩니다.

<Canvas>
  <Story name="default">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="default" />

<br />
### Color List

<Story name="color">
  <div className="flex gap-4 p-2">
    <Button color="twitter">Twitter</Button>
    <Button color="orange">Orange</Button>
    <Button color="red">Red</Button>
    <Button color="blue">Blue</Button>
    <Button color="black">Black</Button>
  </div>
</Story>

<br />
### Outline

<Story name="outline">
  <div className="flex gap-4 bg-black p-2">
    <Button color="twitter" outline>
      Twitter
    </Button>
    <Button color="orange" outline>
      Orange
    </Button>
    <Button color="red" outline>
      Red
    </Button>
    <Button color="blue" outline>
      Blue
    </Button>
    <Button color="black" outline>
      Black
    </Button>
  </div>
</Story>

<br />
### fullWidth

<Story name="fullWidth">
  <Button color="twitter" fullWidth>
    Twitter
  </Button>
</Story>
